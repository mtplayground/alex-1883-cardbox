export function HomeView() {
  return (
    <section className="placeholder-panel" aria-labelledby="placeholder-title">
      <div className="placeholder-copy">
        <p className="placeholder-eyebrow">Vite + React + TypeScript</p>
        <h2 id="placeholder-title">Project shell is ready</h2>
        <p>
          The application has a typed React entry point, linting, formatting,
          and the folder structure needed for upcoming inbox features.
        </p>
      </div>
    </section>
  );
}
